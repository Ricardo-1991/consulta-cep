import styled from "styled-components";

export const Container = styled.main`
  margin-top: 5rem;
  display: grid;
  place-items: center;
  label {
    text-align: center;
  }

  input {
    padding: 0.5rem;
    margin-top: 2rem;
  }
`;
